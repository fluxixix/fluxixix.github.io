# npm 12 起非 registry 来源的依赖默认被拦下（allow-git / allow-remote 默认 none）。
# 主题是从 GitHub 装的 git 依赖，这一行是必需的；root = 只放行根 package.json
# 里显式声明的 git 依赖，依赖树里别人带进来的仍然被拦。
# npm 11 及更早会把它当成未知配置（一条 warning），可以留着不管。
allow-git=root
