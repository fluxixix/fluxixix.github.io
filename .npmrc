# npm 12 起非 registry 来源的依赖默认被拦下（allow-git / allow-remote 默认 none）。
# 主题是从 GitHub 装的 git 依赖，这一行是必需的。
#
# 这里必须写 all，不能写 root。root 的判定是"根 package.json 里声明的 git 依赖"，
# 但改了 ref 之后再 install，npm 重新解析这一步走的是另一条路径，会被当成 non-root
# fetch 直接拦下（EALLOWGIT），也就是"装得上、升不动"：
#
#   npm i -D "…vitepress-theme-fluxixix.git#v0.4.0"   # 首次：root 也能过
#   # 之后每次改 ref 再 npm install 都会失败，只能删掉 node_modules 重装
#
# npm 12 的 allow-git 只有 none / root / all 三个值，没有按包名或 glob 放行的写法，
# 所以想让"改 ref → npm install"这条路走得通，只能给到 all。依赖树里唯一来自 git 的
# 就是本主题（它自己的依赖都在 registry 上），实际放宽的范围就到这里。
# npm 11 及更早会把它当成未知配置（一条 warning），可以留着不管。
allow-git=all
